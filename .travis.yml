language: node_js

node_js:
  - 6
  - 8
  - 10

cache:
  directories:
    - $HOME/.npm

jobs:
  include:
    - stage: release
      if: branch = master
      script: skip
      deploy:
        provider: script
        skip_cleanup: true
        script:
          - npx semantic-release
